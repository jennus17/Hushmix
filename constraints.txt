"""Constraints matching the versions this release was built and tested against.

The ranges in ``requirements.txt`` allow newer releases of every dependency, so a
binary built next month can differ from one built today - and the published
checksum legitimately changes with it. Use this file when you want a build that
matches a specific release:

    pip install -r requirements.txt -c constraints.txt

It pins the interpreters and libraries that were actually used, including the
transitive packages that ``pyautogui``, ``pystray`` and ``pycaw`` pull in, because
those affect the binary too. PyInstaller is included: the executable's layout
depends on its version.

Regenerate after upgrading a dependency:

    pip freeze > build/freeze.txt   # review, then copy the pins here
"""

# --- audio -------------------------------------------------------------------
pycaw==20251023
comtypes==1.4.16
pywin32==312
pywin32-ctypes==0.2.3

# --- mixer -------------------------------------------------------------------
pyserial==3.5

# --- system ------------------------------------------------------------------
psutil==7.2.2

# --- GUI ---------------------------------------------------------------------
customtkinter==6.0.0
darkdetect==0.8.0
pystray==0.19.5
pillow==12.3.0
six==1.17.0

# --- updates -----------------------------------------------------------------
requests==2.34.2
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.20
urllib3==2.8.0
pefile==2024.8.26

# --- button actions ----------------------------------------------------------
pyautogui==0.9.54
MouseInfo==0.1.3
PyGetWindow==0.0.9
PyMsgBox==2.0.1
pyperclip==1.11.0
PyRect==0.2.0
PyScreeze==1.0.1
pytweening==1.2.0

# --- build -------------------------------------------------------------------
pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
altgraph==0.17.5
packaging==26.3
